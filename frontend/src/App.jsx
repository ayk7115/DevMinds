// App.jsx is now a shell. All routes are handled in main.jsx via react-router-dom.
// Dashboard → /dashboard (DashboardPage.jsx)
// Homepage → / (HomePage.jsx)
export default function App() { return null; }
